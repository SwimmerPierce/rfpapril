---
phase: "01"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["pyproject.toml", "requirements.txt", "src/database/models.py", "src/database/session.py", "alembic.ini", "migrations/env.py", ".env.example"]
autonomous: true
requirements: ["DB-01", "DB-02", "DB-03"]
must_haves:
  truths:
    - "Database models for Project, Company, and Bid exist with correct relational mapping"
    - "Alembic is configured and can generate/run migrations against a local Postgres"
    - "Project environment is reproducible with pyproject.toml and requirements.txt"
  artifacts:
    - path: "src/database/models.py"
      provides: "SQLModel definitions for core entities"
    - path: "alembic.ini"
      provides: "Migration tool configuration"
    - path: "src/database/session.py"
      provides: "Database engine and session factory"
  key_links:
    - from: "src/database/models.py"
      to: "PostgreSQL"
      via: "SQLModel"
      pattern: "class .*\\(SQLModel, table=True\\)"
---

<objective>
Establish the Python development environment and define the relational schema for projects, companies, and bids.

Purpose: Provide a solid relational foundation for the scraper to persist data and ensure deduplication.
Output: Reproducible project skeleton, SQLModel definitions, and Alembic migration scaffold.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Boilerplate Setup</name>
  <files>pyproject.toml, requirements.txt, .env.example, README.md</files>
  <action>
    Initialize a Python 3.11+ project with SQLModel, Alembic, Playwright, and Pytest.
    - Create pyproject.toml with metadata and dependencies.
    - Create requirements.txt for simple deployment compatibility.
    - Create .env.example with DATABASE_URL placeholder.
    - Create basic README.md with setup instructions.
  </action>
  <verify>
    <automated>python --version && pip install -r requirements.txt --dry-run</automated>
  </verify>
  <done>Project configuration files exist and are syntactically correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Data Models & Session Logic</name>
  <files>src/database/models.py, src/database/session.py</files>
  <behavior>
    - Project: Unique on opportunity_id (string). Fields: name, issuing_org, date, url.
    - Company: Unique on legal_name (string, case-insensitive normalization). Fields: google_found, website_found, linkedin_found.
    - Bid: Foreign keys to Project and Company. Fields: amount (numeric), is_winner (boolean).
    - SystemError: Persistent log for failures (source, error_message, timestamp, resolved).
  </behavior>
  <action>
    Create SQLModel definitions for the four core tables.
    Implement database session factory in src/database/session.py using DATABASE_URL from environment.
    Ensure Company legal_name is indexed and enforced as unique.
  </action>
  <verify>
    <automated>pytest -v tests/test_models.py (Note: Wave 0 task for tests needed if not using TDD plan)</automated>
  </verify>
  <done>Models defined and session factory handles connection correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Alembic Migration Scaffold</name>
  <files>alembic.ini, migrations/env.py, migrations/script.py.mako</files>
  <action>
    Initialize Alembic in the project root.
    Configure migrations/env.py to import SQLModel's metadata from src/database/models.py for auto-generation.
    Ensure migrations correctly handle Postgres schemas if needed (default to 'public').
  </action>
  <verify>
    <automated>alembic revision --autogenerate -m "Initial migration" --dry-run</automated>
  </verify>
  <done>Alembic is configured to detect SQLModel changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment → App | Configuration and secrets (DB credentials) cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / Env Vars | mitigate | Use .env.example only; actual secrets in GitHub Secrets and environment config |
| T-01-02 | Tampering | DB Models | mitigate | Use SQLModel (Pydantic) for strong typing and validation |
</threat_model>

<verification>
Run `alembic revision --autogenerate` to ensure models are correctly mapped to metadata.
</verification>

<success_criteria>
1. All models (Project, Company, Bid, SystemError) defined with correct relations.
2. Alembic detects models and can generate a migration script.
3. Database connection factory handles environment variables.
</success_criteria>

<output>
After completion, create .planning/phases/01-database-scraper-skeleton/01-01-SUMMARY.md
</output>
