---
phase: "01"
plan: "03"
type: "execute"
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: ["src/scraper/bc_bids.py", "src/main.py", "src/scraper/parser.py"]
autonomous: true
requirements: ["SCRAP-02", "DB-03"]
must_haves:
  truths:
    - "Scraper can navigate to the BC Bids Unverified Results page"
    - "Scraper extracts Opportunity ID, Bidder Name, and Bid Amount accurately"
    - "New records are saved to Postgres while existing records (by Opportunity ID + Company) are skipped or updated (Upsert)"
    - "Scrape failures or data inconsistencies are recorded in the SystemError table"
  artifacts:
    - path: "src/scraper/bc_bids.py"
      provides: "Playwright navigation and extraction logic"
    - path: "src/main.py"
      provides: "Application entrypoint and task orchestration"
    - path: "src/scraper/parser.py"
      provides: "Clean extraction logic from HTML into data models"
  key_links:
    - from: "src/scraper/bc_bids.py"
      to: "BC Bids website"
      via: "Playwright"
      pattern: "page\\.goto\\(.*showUnverifiedBidResults.*\\)"
    - from: "src/scraper/bc_bids.py"
      to: "src/database/models.py"
      via: "SQLModel"
      pattern: "from src.database.models import Project, Company, Bid"
---

<objective>
Implement the core scraping engine to pull unverified bid results from BC Bids and persist them to the database with deduplication logic.

Purpose: Provide the daily data feed that powers the entire lead generation system.
Output: Working scraper script, entrypoint for cron execution, and robust deduplication logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-database-scraper-skeleton/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BC Bids Extraction Logic</name>
  <files>src/scraper/bc_bids.py, src/scraper/parser.py</files>
  <behavior>
    - Scraper navigates to https://www.bcbid.gov.bc.ca/open.dll/showUnverifiedBidResults?mid=1
    - Scraper handles ASP.NET viewstate and waits for the table to load.
    - Parser extracts: Opportunity ID (string), Project Name (string), Bidder Name (string), Bid Amount (numeric).
    - Handle pagination if unverified results span multiple pages.
  </behavior>
  <action>
    Implement Playwright logic to navigate and extract data from the BC Bids unverified results table.
    Use CSS selectors or XPaths to isolate the results rows.
    Normalize strings (strip whitespace, consistent casing for legal names).
  </action>
  <verify>
    <automated>pytest tests/test_scraper.py</automated>
  </verify>
  <done>Scraper can pull raw data from the unverified results page into normalized dictionaries/objects.</done>
</task>

<task type="auto">
  <name>Task 2: Persistence & Deduplication</name>
  <files>src/scraper/processor.py, src/main.py</files>
  <action>
    Implement logic to save extracted results to the database:
    1. Check if Project exists by opportunity_id; create if new.
    2. Check if Company exists by legal_name (normalized); create if new.
    3. Create Bid record linking Project and Company.
    4. Implement Upsert logic: If a Bid for the same Project and Company exists, update the amount if changed, otherwise skip to prevent duplicates.
    Ensure all operations are wrapped in a transaction.
  </action>
  <verify>
    <automated>python -m src.main --test-run</automated>
  </verify>
  <done>Scraper successfully saves unique records to the database without duplicates.</done>
</task>

<task type="auto">
  <name>Task 3: Failure Handling & Entrypoint</name>
  <files>src/main.py</files>
  <action>
    Wrap the scraping process in robust try-except blocks.
    On failure:
    - Capture the traceback and context (e.g., current URL, error type).
    - Create a record in the SystemError table (source='scraper', error_message=..., status='pending').
    - Log the error to stdout for container logging.
    Implement CLI arguments to trigger a full scrape or a dry run.
  </action>
  <verify>
    <automated>python -m src.main --trigger-failure</automated>
  </verify>
  <done>Failures are captured in the database, and src/main.py is ready for 3 AM cron execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BC Bids → Scraper | Untrusted HTML data being parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Scraper Parser | mitigate | Validate extracted data with Pydantic/SQLModel before DB insertion |
| T-01-06 | Denial of Service | BC Bids | mitigate | Implement random delays and respect robots.txt (if applicable) / avoid aggressive hammering |
| T-01-07 | Information Disclosure | Scraper Logs | mitigate | Ensure PII is not logged in SystemError table if it's sensitive (e.g. credentials) |
</threat_model>

<verification>
Run a manual test execution and verify records in PostgreSQL via `psql`.
</verification>

<success_criteria>
1. Scraper pulls correct data from BC Bids.
2. PostgreSQL contains Project, Company, and Bid records after execution.
3. No duplicate Bids exist for the same (Project, Company) pair.
4. Failures are recorded in the SystemError table.
</success_criteria>

<output>
After completion, create .planning/phases/01-database-scraper-skeleton/01-03-SUMMARY.md
</output>
