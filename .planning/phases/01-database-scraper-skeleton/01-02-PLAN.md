---
phase: "01"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified: [".github/workflows/deploy.yml", "railway.json", "Dockerfile"]
autonomous: true
requirements: ["OPS-01", "SCRAP-01"]
user_setup:
  - service: "Railway/Render"
    why: "Deployment platform for Python app and Postgres"
    env_vars:
      - name: "DATABASE_URL"
        source: "Railway/Render Dashboard"
      - name: "RAILWAY_TOKEN"
        source: "Railway Dashboard -> Settings -> Tokens"
must_haves:
  truths:
    - "Application can be built into a Docker container"
    - "GitHub Actions pipeline triggers on push to main"
    - "Infrastructure definition includes a cron schedule for the scraper"
  artifacts:
    - path: "Dockerfile"
      provides: "Containerized environment for Python/Playwright"
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline"
    - path: "railway.json"
      provides: "Deployment configuration"
  key_links:
    - from: "GitHub"
      to: "Railway/Render"
      via: "GitHub Action / Webhook"
      pattern: "uses: .*/railway-deploy"
---

<objective>
Configure the continuous integration and deployment pipeline, and set up the production infrastructure on Railway/Render.

Purpose: Automate the deployment process and ensure the scraper runs on the required schedule.
Output: Dockerfile, GitHub Actions workflow, and infrastructure configuration files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerize Application</name>
  <files>Dockerfile</files>
  <action>
    Create a Dockerfile using a Python 3.11-slim base image.
    Install system dependencies required by Playwright (libnss3, libatk, libpango, etc.).
    Set up working directory, install Python dependencies from requirements.txt, and install Playwright browsers.
    Define CMD as the entrypoint for the scraper (placeholder to be created in 01-03).
  </action>
  <verify>
    <automated>docker build -t bc-bids-scraper . --dry-run</automated>
  </verify>
  <done>Dockerfile is valid and installs necessary scraper dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Configure CI/CD Pipeline</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    Create a GitHub Actions workflow that:
    - Triggers on push to 'main' branch.
    - Runs linting (flake8/black) and tests (pytest).
    - Deploys to Railway (using railwayapp/action-deploy or similar) or Render.
    - Passes environment variables like DATABASE_URL as secrets.
  </action>
  <verify>
    <automated>gh workflow run deploy.yml --dry-run</automated>
  </verify>
  <done>GitHub Actions workflow is correctly defined.</done>
</task>

<task type="auto">
  <name>Task 3: Infrastructure & Cron Definition</name>
  <files>railway.json</files>
  <action>
    Create a railway.json (or equivalent Render blueprint) to define the service.
    Configure a health check if needed (though it's a batch job).
    Define the cron schedule "0 3 * * *" (3:00 AM daily) for the main scraper command.
    Ensure it provisions a PostgreSQL instance if not manually created.
  </action>
  <verify>
    <automated>railway config list</automated>
  </verify>
  <done>Infrastructure config includes the 3 AM cron schedule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Production | Deployment credentials and secret injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | GH Actions | mitigate | Use least-privilege tokens for deployment; restrict push to main with branch protection |
| T-01-04 | Denial of Service | Railway Cron | accept | Low risk for daily job; rely on platform uptime and monitoring |
</threat_model>

<verification>
Verify railway.json syntax and GitHub Action YAML syntax.
</verification>

<success_criteria>
1. Dockerfile builds successfully with Playwright dependencies.
2. deploy.yml correctly maps secrets and triggers on main push.
3. railway.json defines the 3 AM cron job correctly.
</success_criteria>

<output>
After completion, create .planning/phases/01-database-scraper-skeleton/01-02-SUMMARY.md
</output>
