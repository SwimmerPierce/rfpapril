---
phase: 02-business-logic-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: [src/main.py, src/processor/pipeline.py, src/processor/error_queue.py, tests/test_pipeline.py]
autonomous: true
requirements: [PROC-01, PROC-02, PROC-03, PROC-04]
must_haves:
  truths:
    - "Scraper execution is immediately followed by business logic processing"
    - "Enrichment is triggered only for companies that haven't been processed yet"
    - "Errors during processing or enrichment are logged to the SystemError table"
  artifacts:
    - path: "src/processor/pipeline.py"
      provides: "High-level orchestration for post-scrape tasks"
    - path: "src/processor/error_queue.py"
      provides: "Centralized error logging for Zoho sync preparation"
  key_links:
    - from: "src/main.py"
      to: "src/processor/pipeline.py"
      via: "Function call in main CLI"
    - from: "src/processor/pipeline.py"
      to: "src/processor/winner_logic.py"
      via: "Function call"
---

<objective>
Coordinate the scraper, logic engine, and enrichment pipeline into a single, reliable daily workflow.

Purpose: To ensure every daily scrape results in enriched, assigned leads.
Output: Integrated pipeline in the main entrypoint and persistent error reporting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-logic-engine/02-RESEARCH.md
@src/main.py
@src/database/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Central Processing Pipeline</name>
  <files>src/processor/pipeline.py, tests/test_pipeline.py</files>
  <action>
    - Create `src/processor/pipeline.py` with `run_post_scrape_pipeline(session: Session)`:
        1. Find all Projects with `date` within the last 24 hours (or projects with unverified bids).
        2. For each project: `flag_winners`.
        3. For each bid in those projects: `assign_brokers`.
        4. Find Companies created in the last 24 hours that haven't been enriched (`website_found` is None).
        5. For each company: `enrich_company`.
    - Create `tests/test_pipeline.py` to verify the end-to-end integration of these services.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline.py</automated>
  </verify>
  <done>Central pipeline logic is defined and integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Pipeline into Main Flow</name>
  <files>src/main.py</files>
  <action>
    - Update `src/main.py` to call `run_post_scrape_pipeline` after the scraper completes.
    - Ensure it runs within the same (or a fresh) database session.
    - Wrap the entire post-scrape process in a try-except block to prevent a logic failure from crashing the scraper's final report.
  </action>
  <verify>
    <automated>python src/main.py --help</automated>
  </verify>
  <done>Main entrypoint now orchestrates the full end-to-end data lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Persistent Error Logging (ADMIN-01)</name>
  <files>src/processor/error_queue.py, tests/test_pipeline.py</files>
  <action>
    - Implement `log_processing_error(session, source, message, entity_id)` in `src/processor/error_queue.py`.
    - It must insert a record into the `SystemError` table.
    - Use this in `pipeline.py` to capture:
        - Organization mapping failures (no broker found).
        - Enrichment failures (DDG rate limits or website timeouts).
    - This prepares data for the Phase 3 Zoho Admin Queue.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline.py -k error_logging</automated>
  </verify>
  <done>Failures are captured as persistent items in the database for admin review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → Services | Orchestration involves multiple service boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Availability | Pipeline | mitigate | Use comprehensive try-except blocks and persistent error logging. |
| T-02-03-02 | Reliability | Pipeline | mitigate | Ensure enrichment doesn't retry infinitely on dead sites. |
</threat_model>

<verification>
1. Run `python src/main.py` (with a small mock/test run) to ensure all services fire.
2. Check `SystemError` table for any logged issues.
</verification>

<success_criteria>
- [ ] Daily scrape triggers full business logic.
- [ ] New companies are queued for enrichment.
- [ ] All logical failures are recorded in `SystemError`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-engine/02-03-SUMMARY.md`
</output>
