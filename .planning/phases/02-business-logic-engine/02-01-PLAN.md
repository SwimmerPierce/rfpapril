---
phase: 02-business-logic-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/database/models.py, src/processor/winner_logic.py, src/processor/broker_engine.py, tests/test_logic.py]
autonomous: true
requirements: [PROC-01, PROC-02, PROC-03]
must_haves:
  truths:
    - "System identifies the lowest bid amount for a project and marks it as is_winner=True"
    - "System identifies all brokers assigned to an issuing organization with matching bid thresholds"
    - "System creates records linking bids to their assigned brokers"
  artifacts:
    - path: "src/database/models.py"
      provides: "Broker and BrokerMapping data structures"
    - path: "src/processor/winner_logic.py"
      provides: "Winner flagging logic"
    - path: "src/processor/broker_engine.py"
      provides: "Broker assignment logic"
  key_links:
    - from: "src/processor/winner_logic.py"
      to: "src/database/models.py"
      via: "SQLModel select/update"
    - from: "src/processor/broker_engine.py"
      to: "src/database/models.py"
      via: "SQLModel join between Bid, Project, and BrokerMapping"
---

<objective>
Establish the core database models for insurance brokers and implement the business logic for winner identification and lead assignment.

Purpose: To transform raw bid data into actionable insights for specific brokers.
Output: Updated database schema and tested logic services.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-logic-engine/02-RESEARCH.md
@src/database/models.py

<interfaces>
From src/database/models.py:
```python
class Project(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    issuing_org: str
    ...

class Bid(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    amount: Decimal = Field(default=Decimal("0.0"), decimal_places=2)
    is_winner: bool = Field(default=False)
    project_id: int = Field(foreign_key="project.id")
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Broker and BrokerMapping Models</name>
  <files>src/database/models.py</files>
  <action>
    - Add `Broker` model: id (int), name (str), email (str), is_active (bool, default=True).
    - Add `BrokerMapping` model: id (int), issuing_org (str), broker_id (int, FK), min_threshold (Decimal), max_threshold (Decimal).
    - Add `BidAssignment` link table: bid_id (int, FK), broker_id (int, FK) for a many-to-many relationship (though often 1:1, this allows flexibility).
    - Update `Bid` model to have a relationship to `Broker` through `BidAssignment`.
    - Note: This implements the "recommended models" from research (D-01/D-02 equivalent).
  </action>
  <verify>
    <automated>alembic revision --autogenerate -m "Add broker models" && alembic upgrade head</automated>
  </verify>
  <done>Database schema updated with Broker and BrokerMapping tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Winner Identification Logic (PROC-01)</name>
  <files>src/processor/winner_logic.py, tests/test_logic.py</files>
  <behavior>
    - Given a list of Bids for a Project, identify the one with the lowest `amount`.
    - Set `is_winner=True` for that bid, and `is_winner=False` for others.
    - If multiple bids share the same lowest amount, all are marked as winners (rare edge case).
  </behavior>
  <action>
    - Create `tests/test_logic.py` with `test_flag_winners`.
    - Implement `flag_winners(session: Session, project_id: int)` in `src/processor/winner_logic.py`.
    - Follow the SQL-optimized approach from RESEARCH.md: `min_amount = select(func.min(Bid.amount)).where(...)`.
  </action>
  <verify>
    <automated>pytest tests/test_logic.py::test_flag_winners</automated>
  </verify>
  <done>Logic correctly flags the lowest bid as the winner in the database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Broker Assignment Engine (PROC-02, PROC-03)</name>
  <files>src/processor/broker_engine.py, tests/test_logic.py</files>
  <behavior>
    - For a given Bid, find its Project's `issuing_org`.
    - Query `BrokerMapping` for matching `issuing_org` and `bid.amount` within `[min_threshold, max_threshold]`.
    - Create `BidAssignment` entries for all matching brokers.
    - If no mapping exists, do nothing (Phase 3 will handle the "unmapped org" queue).
  </behavior>
  <action>
    - Add `test_broker_assignment` to `tests/test_logic.py`.
    - Implement `assign_brokers(session: Session, bid_id: int)` in `src/processor/broker_engine.py`.
    - Ensure it handles cases with no matches, one match, and multiple matches.
  </action>
  <verify>
    <automated>pytest tests/test_logic.py::test_broker_assignment</automated>
  </verify>
  <done>Bids are correctly linked to Brokers based on organization and amount thresholds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logic Engine → DB | All queries are parameterized via SQLModel to prevent injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `winner_logic.py` | mitigate | Use SQLModel built-in parameterized queries for all calculations. |
| T-02-01-02 | Information Disclosure | `Broker` table | mitigate | Ensure email addresses are only accessible to internal services. |
</threat_model>

<verification>
1. Run `pytest tests/test_logic.py` to ensure both winner and broker logic pass.
2. Manually verify `test.db` schema includes the new tables.
</verification>

<success_criteria>
- [ ] Winner logic flags lowest bid correctly.
- [ ] Broker assignment matches organization and threshold rules.
- [ ] Database schema successfully reflects Broker-related entities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-engine/02-01-SUMMARY.md`
</output>
