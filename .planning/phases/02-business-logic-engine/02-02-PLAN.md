---
phase: 02-business-logic-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/processor/enrichment.py, tests/test_enrichment.py]
autonomous: true
requirements: [PROC-04]
must_haves:
  truths:
    - "System finds an official website for a given company name using DuckDuckGo"
    - "System extracts a LinkedIn company profile URL from the found website"
    - "System updates the Company record with website and LinkedIn URLs"
  artifacts:
    - path: "src/processor/enrichment.py"
      provides: "Company enrichment pipeline"
    - path: "tests/test_enrichment.py"
      provides: "TDD suite for search and extraction"
  key_links:
    - from: "src/processor/enrichment.py"
      to: "DuckDuckGo Search"
      via: "duckduckgo-search library"
    - from: "src/processor/enrichment.py"
      to: "Company Website"
      via: "requests and BeautifulSoup"
---

<objective>
Implement the company enrichment pipeline to automatically discover company websites and LinkedIn profiles for newly identified bidders.

Purpose: To provide brokers with contact context for lead outreach.
Output: Enrichment service and validated discovery logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-logic-engine/02-RESEARCH.md

<interfaces>
From src/database/models.py:
```python
class Company(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    legal_name: str = Field(unique=True, index=True)
    website_found: Optional[str] = Field(default=None)
    linkedin_found: Optional[str] = Field(default=None)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Website Discovery (PROC-04)</name>
  <files>src/processor/enrichment.py, tests/test_enrichment.py</files>
  <behavior>
    - Given a company legal name, search DuckDuckGo for its official website.
    - Exclude common noise sites like LinkedIn, Facebook, YellowPages, etc., in the search results to find the *actual* company domain.
    - Return the most likely URL.
  </behavior>
  <action>
    - Create `tests/test_enrichment.py` with `test_find_website`.
    - Implement `find_website(company_name: str) -> Optional[str]` in `src/processor/enrichment.py` using `duckduckgo-search`.
    - Use the pattern from RESEARCH.md: `ddgs.text(f"{company_name} official website", max_results=3)`.
  </action>
  <verify>
    <automated>pytest tests/test_enrichment.py::test_find_website</automated>
  </verify>
  <done>Service successfully identifies company websites from legal names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LinkedIn Extraction (PROC-04)</name>
  <files>src/processor/enrichment.py, tests/test_enrichment.py</files>
  <behavior>
    - Given a company website URL, fetch the homepage HTML.
    - Parse the HTML using BeautifulSoup to find any link containing `linkedin.com/company/`.
    - Return the first LinkedIn company profile URL found.
  </behavior>
  <action>
    - Add `test_extract_linkedin` to `tests/test_enrichment.py`.
    - Implement `extract_linkedin(website_url: str) -> Optional[str]` in `src/processor/enrichment.py`.
    - Handle network timeouts and basic HTML parsing errors gracefully.
  </action>
  <verify>
    <automated>pytest tests/test_enrichment.py::test_extract_linkedin</automated>
  </verify>
  <done>Service successfully extracts LinkedIn profile links from company homepages.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Enrichment Orchestrator</name>
  <files>src/processor/enrichment.py</files>
  <action>
    - Implement `enrich_company(session: Session, company_id: int)`:
        1. Fetch company by ID.
        2. Run `find_website`.
        3. If website found, update `website_found` and run `extract_linkedin`.
        4. Update `linkedin_found` if match found.
        5. Commit changes.
  </action>
  <verify>
    <automated>pytest tests/test_enrichment.py::test_enrich_company</automated>
  </verify>
  <done>Full enrichment flow for a single company is implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Internet | External network calls to DDG and arbitrary company websites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Denial of Service | Enrichment Engine | mitigate | Implement timeouts on all `requests.get` calls (e.g., 10s). |
| T-02-02-02 | Information Disclosure | `Company` table | mitigate | Do not store sensitive data from scraped sites; only URLs. |
</threat_model>

<verification>
1. Run `pytest tests/test_enrichment.py` to verify the discovery logic.
</verification>

<success_criteria>
- [ ] Website discovery finds correct domains for 3-5 sample BC companies.
- [ ] LinkedIn extraction correctly pulls URLs from known sites.
- [ ] Enrichment updates the database without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-engine/02-02-SUMMARY.md`
</output>
